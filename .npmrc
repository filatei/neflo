# next-auth@5 beta declares @simplewebauthn@^9 as an OPTIONAL peer (for its own
# WebAuthn provider, which we don't use). We use @simplewebauthn@13 directly for
# TapPay passkeys, so relax strict peer-dep resolution to avoid a false conflict.
legacy-peer-deps=true
